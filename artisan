#!/usr/bin/env php
<?php

define('LARAVEL_START', microtime(true));

/*
|--------------------------------------------------------------------------
| 자동 로더 등록
|------------------------------------------------- -------------------------
|
| Composer는 편리한 자동 생성 클래스 로더를 제공합니다.
| 우리의 응용 프로그램을 위해. 우리는 그것을 활용하기만 하면 됩니다! 우리는 그것을 요구할 것입니다
| 여기에 스크립트를 입력하면
| 모든 클래스를 "수동으로" 로드합니다. 휴식을 취하기에 좋습니다.
|
*/

require __DIR__ . '/vendor/autoload.php';

$app = require_once __DIR__ . '/bootstrap/app.php';

/*
|--------------------------------------------------------------------------
| Artisan 애플리케이션 실행
|------------------------------------------------- -------------------------
|
| 콘솔 애플리케이션을 실행할 때 현재 CLI 명령은
| 이 콘솔에서 실행되고 응답이 터미널로 다시 전송됨
| 또는 개발자를 위한 다른 출력 장치. 아무것도 가지 않는다!
|
*/

$kernel = $app->make(Illuminate\Contracts\Console\Kernel::class);

$status = $kernel->handle(
  $input = new Symfony\Component\Console\Input\ArgvInput,
  new Symfony\Component\Console\Output\ConsoleOutput
);

/*
|--------------------------------------------------------------------------
| 애플리케이션 종료
|------------------------------------------------- -------------------------
|
| Artisan 실행이 완료되면 종료 이벤트를 시작합니다.
| 종료하기 전에 응용 프로그램에서 최종 작업을 수행할 수 있도록
| 프로세스 아래로. 이것은 요청에 발생하는 마지막 일입니다.
|
*/

$kernel->terminate($input, $status);

exit($status);
